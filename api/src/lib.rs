mod column;
mod data_row;
mod database;
mod internal;
mod row;
mod type_;
mod value;

pub(crate) fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
